# OpenCraft 智能工作台 — 用户前端呈现 PRD

> 版本：v1.0
> 日期：2026-04-23
> 状态：评审中

---

## 1. 产品定位与核心体验

OpenCraft 是一个面向数字孪生场景的**智能微应用编排工作台**。用户通过自然语言对话与 AI 协作，快速调用、组合、创建微应用，实现对电力/园区等场景的监控、诊断与预测。

**核心体验原则：**
- **对话即入口**：一切操作从对话开始，降低使用门槛
- **所见即所得**：微应用的接口、流程、执行结果即时可见
- **上下文连贯**：通用对话与专属对话无缝切换，历史消息持久化
- **零学习成本**：界面自解释，hover 即提示，操作有反馈

---

## 2. 整体布局架构

采用经典的三栏弹性布局，在 1440px 及以上宽度下达到最佳体验。

```
┌─────────────────────────────────────────────────────────────┐
│  Sidebar (280px)  │  Main (flex-1)      │  Aside (320px)   │
│                   │                      │                  │
│  ┌─────────────┐  │  ┌───────────────┐  │  ┌───────────┐  │
│  │ AppList     │  │  │ ChatWindow    │  │  │ ApiList   │  │
│  │ · 收藏夹    │  │  │ · 欢迎卡片    │  │  │ · Tabs    │  │
│  │ · 系统预置  │  │  │ · 消息列表    │  │  │ · 搜索    │  │
│  │             │  │  │ · 快捷标签    │  │  │ · 列表    │  │
│  │             │  │  │ · 输入框      │  │  │           │  │
│  └─────────────┘  │  └───────────────┘  │  └───────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.1 布局规则

| 区域 | 宽度 | 最小宽度 | 行为 |
|------|------|----------|------|
| Sidebar | 280px | 240px | 固定宽度，可折叠为 64px（仅显示图标） |
| Main | flex-1 | 560px | 自适应填充剩余空间 |
| Aside | 320px | 280px | 默认展开，可折叠为 44px（仅保留展开按钮） |

**响应式断点：**
- `≥1440px`：三栏完整显示
- `1280px ~ 1439px`：Aside 默认折叠，hover 时展开
- `<1280px`：进入平板模式，Sidebar 变为抽屉式，Main 全宽

---

## 3. 模块详细设计

### 3.1 Sidebar — 微应用导航（AppList）

**核心职责**：让用户快速找到并切换微应用，管理个人收藏。

#### 3.1.1 信息架构

```
Sidebar
├── Header（品牌标识 + 新建按钮）
├── 收藏夹（用户自定义 microApps）
│   ├── 空态：引导创建
│   └── 列表项：hover 显示详情浮层
├── 系统预置（平台内置 microApps）
│   ├── 空态：异常提示
│   └── 列表项：hover 显示详情浮层
└── Footer（可选：版本信息 / 帮助入口）
```

#### 3.1.2 列表项设计

```tsx
interface AppListItemProps {
  app: MicroApp;
  isSelected: boolean;
  isEditing: boolean;
}
```

**视觉规范：**
- 高度：自适应（内容 + 12px 上下内边距）
- 圆角：6px
- 默认态：bg-white，hover:bg-[#f2f3f5]
- 选中态：bg-[#e8f3ff]，name 文字变主色 #165dff
- 图标：收藏夹用 🔧，系统预置用 📦（未来替换为统一 SVG 图标库）
- 右侧徽标：接口数量（11px 字号，#86909c，bg-[#f2f3f5]）

**操作按钮（hover 时浮现）：**
- 重命名：✎（编辑态切换为 input + 确认/取消）
- 删除：×（二次确认 dialog）

#### 3.1.3 Tooltip 浮层（关键交互）

**为什么重要**：微应用名称通常较短（如"避雷器动作监视"），但用户需要了解其触发条件、绑定接口、执行逻辑才能决策是否点击。

**实现方案**：Portal + Fixed 定位 + ResizeObserver
- 脱离 overflow 容器限制，彻底解决被裁剪问题
- 根据视口空间自动决定向上/向下展开
- 内容过多时内部局部滚动，不撑破视口

**浮层内容结构：**
```
┌─────────────────────────────┐
│ 应用名称（14px semibold）    │
│ 应用描述（12px #4e5969）     │
│ 触发：xxx（11px #86909c）    │
│ ┌─────┐ ┌─────┐            │
│ │3个接口│ │定时任务│          │
│ └─────┘ └─────┘            │
└─────────────────────────────┘
```

#### 3.1.4 空态设计

**收藏夹为空时：**
- 图标：🔧（36px）
- 文案："暂无微应用" / "在对话中输入"帮我建一个..."来创建"
- 行动点：高亮关键词，暗示自然语言入口

---

### 3.2 Main — 智能对话区（ChatWindow）

**核心职责**：承载人机对话、微应用执行结果展示、创建确认流程。

#### 3.2.1 双模式架构

ChatWindow 支持两种互斥模式，通过左侧 Sidebar 的选择触发切换：

| 模式 | 触发条件 | 标题标识 | 上下文 |
|------|----------|----------|--------|
| 通用对话 | 未选择微应用 | "通用对话"（蓝底标签） | messages[]，持久化到 localStorage |
| 专属对话 | 选择了某个微应用 | "专属对话"（绿底标签） | dedicatedMessages[]，切换应用即清空 |

**模式切换动效**：
- 切换时 header 标签做 150ms 的 scale 脉冲动画（1 → 1.05 → 1）
- 消息列表区域做 200ms 的淡入淡出（opacity 0→1）

#### 3.2.2 Header 区域

```
┌──────────────────────────────────────────────────────────┐
│ ⚙️ OpenCraft 智能助手  [通用对话] [当前场景]        [新建] │
│                                              [自定义JSON] [状态]│
└──────────────────────────────────────────────────────────┘
```

**元素说明：**
- 标题：固定"⚙️ OpenCraft 智能助手"或当前微应用名称
- 标签："通用对话"（#165dff 底色）或"专属对话"（#00b42a 底色）
- 场景标签：currentDomain（如"电网数字孪生"），#722ed1 底色，仅在非空时显示
- 操作按钮：新建微应用（通用模式）、返回通用对话（专属模式）、自定义 JSON、系统状态指示

#### 3.2.3 欢迎卡片（仅通用模式）

**设计意图**：降低冷启动焦虑，告知用户当前能力边界。

```
┌──────────────────────────────────────────────────────────┐
│ 渐变背景（135deg, #165dff → #114ec2）                    │
│                                                          │
│  欢迎使用数字孪生智能助手                                 │
│  当前场景为 电网数字孪生，共有 12 个微应用、8 个 API 接口   │
│  您可以向我提问任何问题，我会自动识别并调用对应的微应用      │
│                                                          │
│  [避雷器监视] [线路温度] [变电站巡检] ...                  │
└──────────────────────────────────────────────────────────┘
```

**规范：**
- 圆角：12px
- 内边距：28px
- 快捷标签：最多显示 8 个，点击即发送对应提问
- 标签样式：bg rgba(255,255,255,0.15)，hover 时 bg rgba(255,255,255,0.25)

#### 3.2.4 消息列表

**消息气泡规范：**

| 属性 | 用户消息 | AI 消息 |
|------|----------|---------|
| 背景 | #165dff | #f2f3f5 |
| 文字颜色 | white | #1a1a1a |
| 圆角 | 12px（右下 4px） | 12px（左下 4px） |
| 最大宽度 | 85% | 85% |
| 头像 | 👤（蓝底白字） | 🤖（灰底） |

**内容渲染规则：**
- Markdown 语法：支持 `**粗体**`、代码块 ```json
- 代码块带"复制"按钮，点击后 1.5s 内显示"已复制"
- 超链接自动识别并加下划线
- 长文本自动换行（break-words）

**流式输出态：**
- AI 消息初始带 `_streaming: true`
- 显示三个跳动圆点（animate-bounce）
- 内容增量更新时保持滚动跟随（仅在用户未手动上滚时）

#### 3.2.5 微应用执行确认卡片

当 AI 识别到用户意图是创建微应用时，返回包含 `[CONFIRM_CREATE_APP]` 标记的消息，前端解析后展示确认卡片。

```
┌──────────────────────────────────────────────────────────┐
│ 🛠️ 确认创建微应用                              [x关闭]   │
├──────────────────────────────────────────────────────────┤
│ [基本信息] [代码预览] [渲染预览]                          │
├──────────────────────────────────────────────────────────┤
│ 应用名称： [__________]                                  │
│ 应用描述： [__________]                                  │
│ 包含接口： [接口A] [接口B]                               │
│ 调用流程：                                               │
│   ① 查询避雷器状态                                       │
│   ② 分析动作次数   [x]                                   │
│                                                          │
│                                   [取消]    [✓ 确认创建] │
└──────────────────────────────────────────────────────────┘
```

**交互细节：**
- Tabs 切换无刷新，内容区域做 100ms 淡入
- 流程步骤可删除（hover 出现 ×）
- 删除步骤时自动检查接口是否仍被引用，若无引用则同步移除接口标签
- 确认创建后卡片消失，toast 提示"微应用创建成功"

#### 3.2.6 快捷标签栏

位于输入框上方，提供高频问法的一键发送。

**标签来源：**
- 通用模式：根据 currentDomain 动态匹配（电力场景 → 避雷器/缺陷/油色谱...；园区场景 → 安防/能耗/电梯...）
- 专属模式：根据 selectedMicroApp.id 匹配预置标签（line-monitor → 线路温度/覆冰情况...）

**样式：**
- bg-[#f2f3f5]，border-[#e5e6eb]
- hover：border-[#165dff]，text-[#165dff]，bg-[#e8f3ff]
- transition：150ms ease

#### 3.2.7 输入区域

```
┌──────────────────────────────────────────────────────────┐
│  ┌──────────────────────────────────────────────────┐ [发送]│
│  │ 输入你的需求，例如：帮我建一个避雷器动作监视微应用... │    │
│  └──────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
```

**规范：**
- 最小高度：44px，最大高度：120px
- Enter 发送，Shift+Enter 换行
- 发送中禁用输入，按钮显示 loading 态
- 自动聚焦（autoFocus）

---

### 3.3 Aside — 服务目录（ApiList）

**核心职责**：展示平台可用的 API 接口和 Skill，支持搜索、筛选、查看详情，以及在创建微应用时添加到草稿流程。

#### 3.3.1 信息架构

```
Aside
├── Header（服务目录 + 折叠按钮）
├── Tabs（电网数字孪生服务 / 其他服务）
├── Search（实时搜索 + 快捷筛选标签）
└── List（接口卡片 / Skill 卡片）
```

#### 3.3.2 Header 设计（已优化）

**优化前问题**：Header 标题随 Tab 切换变化（"电网数字孪生服务" / "其他服务"），与 Tabs 文字重复，信息冗余。

**优化后**：
- Header 固定显示"服务目录"，职责清晰（面板标识）
- Tabs 负责内容分类切换，不再重复
- 折叠按钮（▶/◀）增加 title 提示"收起/展开"，hover 态更明显

#### 3.3.3 Tabs 设计

```
┌────────────────────┬────────────────────┐
│   电网数字孪生服务  │      其他服务       │
│   （选中：蓝底下线） │   （未选中：灰字）   │
└────────────────────┴────────────────────┘
```

**规范：**
- 高度：40px
- 选中态：text-[#165dff]，border-b-2 border-[#165dff]，font-medium
- 未选中态：text-[#86909c]，hover:text-[#4e5969]
- 切换时触发对应数据加载（懒加载 Skill 列表）

#### 3.3.4 搜索与筛选

**搜索框：**
- placeholder："搜索服务..."
- 实时过滤（debounce 200ms）
- 支持名称、路径、描述三字段匹配

**快捷筛选标签（仅电网数字孪生 Tab）：**
```
[全部] [避雷器] [变电站] [线路] [变压器] [开关柜]
```
- "全部"等价于清空搜索
- 点击后自动填充搜索框并触发过滤
- 选中态：bg-[#165dff] text-white

#### 3.3.5 列表卡片

**API 卡片：**
```
┌─────────────────────────────────────────┐
│ [GET] [接口]  避雷器动作查询      [+添加] │
│ /api/v1/arrester/actions                │
│ 查询避雷器最近 24 小时的动作次数和泄漏电流 │
└─────────────────────────────────────────┘
```

**Skill 卡片：**
```
┌─────────────────────────────────────────┐
│ [Skill]  故障诊断助手                    │
│ 基于录波数据和保护动作信息进行故障定位     │
│ ─────────────────────────────────────── │
│ [详情展开区：代码/描述]                   │
└─────────────────────────────────────────┘
```

**状态规范：**
| 状态 | 边框 | 背景 | 标题颜色 |
|------|------|------|----------|
| 默认 | #e5e6eb | white | #1a1a1a |
| hover | #165dff | #f8f9fa | #1a1a1a |
| 选中（API） | #165dff | #e8f3ff | #165dff |
| 选中（Skill） | #722ed1 | #f5e8ff | #722ed1 |

**方法标签色板：**
- GET：#00b42a（绿）
- POST：#165dff（蓝）
- PUT：#ff7d00（橙）
- DELETE：#f53f3f（红）

#### 3.3.6 "+ 添加"按钮

仅在 pendingCreation（存在待确认的微应用草稿）时显示。

**交互：**
- 点击后将当前 API 添加到草稿流程尾部
- 若接口已存在，toast 提示"该接口已在流程中"（error 态）
- 添加成功，toast 提示"已添加到草稿流程"（success 态）

---

## 4. 专属对话模式下的主内容区

当用户从 Sidebar 选择了某个微应用后，Main 区域进入专属模式，展示更丰富的信息：

### 4.1 执行信息卡片

```
┌──────────────────────────────────────────────────────────┐
│ 执行信息                                    [▶ 执行微应用]│
├──────────────────────────────────────────────────────────┤
│ 触发条件：避雷器动作次数 > 5 次/日                        │
│ 输入参数：stationId, arresterId, timeRange               │
│ 输出结果：actionCount, leakageCurrent, healthScore       │
│ 绑定接口：[避雷器查询] [泄漏电流统计]                      │
│ 关联 Skill：[故障诊断] [寿命预测]                        │
└──────────────────────────────────────────────────────────┘
```

### 4.2 接口调用逻辑（FlowChart）

以可视化流程图展示微应用的执行链：
- 顺序执行：节点纵向排列，箭头 ↓ 连接
- 并行执行：节点横向排列，统一橙色边框标识
- API 节点：左侧方法标签（GET/POST...）+ 右侧名称

**空态**："暂无流程定义"（#86909c，13px）

---

## 5. 全局组件与交互规范

### 5.1 Toast 提示

```
┌────────────────────────────────────┐
│ ✅ 操作成功 / ❌ 操作失败：xxx      │
└────────────────────────────────────┘
```

- 位置：顶部居中，距顶 16px
- 自动消失：3s
- 最多同时显示 1 个（新 toast 覆盖旧的）
- 类型：success（绿）、error（红）、info（蓝）

### 5.2 网络状态指示

位于 Main Header 右侧：
- 在线：绿色圆点 + "系统正常"
- 离线：红色圆点 + "网络断开"
- 切换时圆点做 300ms 的 pulse 动画

### 5.3 滚动行为

| 场景 | 行为 |
|------|------|
| 新消息到达 | 自动滚动到底部（仅当用户未手动上滚时） |
| 切换微应用 | 消息列表强制滚动到底部 |
| 出现确认卡片 | 智能滚动使卡片完全可见 |
| 用户手动上滚 | 停止自动跟随，直到用户回到底部 |

**检测逻辑**：监听 scroll 事件，当 `scrollHeight - scrollTop - clientHeight > 80px` 时判定为用户已手动上滚。

---

## 6. 视觉设计系统（Design Tokens）

### 6.1 色彩体系

```
主色 Primary：    #165dff（品牌蓝，用于主按钮、选中态、链接）
成功 Success：    #00b42a（操作成功、GET 方法）
警告 Warning：    #ff7d00（PUT 方法、定时任务标识）
错误 Error：      #f53f3f（删除、错误提示、DELETE 方法）

文字主色：        #1a1a1a（标题、正文）
文字次要：        #4e5969（描述、副标题）
文字辅助：        #86909c（占位符、时间戳、禁用态）

背景画布：        #f5f6f7（页面底层背景）
背景卡片：        #ffffff（内容区域背景）
背景悬浮：        #f2f3f5（hover 态、标签底色）
背景选中：        #e8f3ff（选中态底色）

边框默认：        #e5e6eb（卡片边框、分割线）
边框强调：        #165dff（选中态边框、focus 态）
```

### 6.2 字体规范

```
字体栈：Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif

标题：    15px, font-semibold, color #1a1a1a
正文：    13px / 14px, font-normal, line-height 1.6
辅助：    11px / 12px, color #86909c
tag：     10px / 11px, 大写或常规
```

### 6.3 阴影与层级

```
卡片阴影：    0 1px 2px rgba(0,0,0,0.04)（默认卡片）
浮层阴影：    0 4px 12px rgba(0,0,0,0.1)（tooltip、dropdown）
模态阴影：    0 8px 24px rgba(0,0,0,0.15)（dialog、确认框）

z-index 层级：
  - 内容层：auto
  - 悬浮层：50（旧 tooltip，待迁移）
  - 固定层：100（header sticky）
  - 遮罩层：500（modal backdrop）
  - 全局浮层：9999（portal tooltip、toast）
```

### 6.4 间距系统

基于 4px 网格：
```
4px   → xs（标签内边距、图标间距）
8px   → sm（元素间小间距）
12px  → md（卡片内边距、按钮内边距）
16px  → base（模块间间距）
20px  → lg（列表项内边距）
24px  → xl（大模块间距）
28px  → 2xl（欢迎卡片内边距）
```

---

## 7. 动画与过渡规范

### 7.1 时长标准

```
快速反馈：100ms（按钮点击态、颜色切换）
常规过渡：150-200ms（hover 浮现、标签切换、面板折叠）
内容切换：200-300ms（模式切换、tab 切换）
页面级：300-400ms（sidebar 展开/折叠）
```

### 7.2 缓动函数

```
默认：ease
弹性：cubic-bezier(0.34, 1.56, 0.64, 1)（toast 进入、标签脉冲）
线性：linear（loading 动画、进度条）
```

### 7.3 具体动画定义

| 动画 | 时长 | 缓动 | 说明 |
|------|------|------|------|
| tooltip 浮现 | 150ms | ease | opacity 0→1，无位移 |
| toast 进入 | 200ms | 弹性 | translateY(-10px)→0 + opacity |
| toast 离开 | 150ms | ease | opacity→0 |
| sidebar 折叠 | 300ms | ease | width 变化 |
| 消息气泡进入 | 150ms | ease | opacity 0→1 + translateY(4px)→0 |
| loading 圆点 | 1s | linear | animate-bounce 无限循环 |

---

## 8. 状态与数据流

### 8.1 全局状态（Zustand）

```
AppState
├── Data（microApps, myMicroApps, apis, skills, samples, currentDomain）
├── Selection（selectedMicroApp, selectedApi, selectedScene）
├── Search（apiSearch, filteredApis）
├── Chat（messages[], dedicatedMessages[], inputText, isThinking）
├── UI（rightPanelView, rightPanelCollapsed, showJsonEditor, toast）
├── Draft（pendingCreation, jsonInput）
└── Loading（各模块 loading 态）
```

### 8.2 本地持久化

```
localStorage key: dtv_messages
value: ChatMessage[]（仅通用对话消息）
说明：页面刷新后自动恢复，切换微应用不清除
```

### 8.3 SSE 流式对话

```
1. 用户点击发送 → 立即渲染用户消息气泡
2. 创建 EventSource → /api/chat/stream?message=xxx&appId=xxx
3. 接收 type=delta → 增量更新 AI 消息内容
4. 接收 type=done → 标记 streaming=false，解析 CONFIRM_CREATE_APP
5. onerror / 超时 → 渲染错误提示消息
6. cleanup → 关闭 EventSource，重置 isThinking
```

---

## 9. 已知问题与待优化清单

### 9.1 已修复

- [x] **Tooltip 被裁剪**：从 absolute 改为 Portal + Fixed + ResizeObserver，彻底脱离 overflow 限制
- [x]** Header 标题重复**：Aside Header 固定为"服务目录"，与 Tabs 职责分离

### 9.2 待优化（高优先级）

- [ ] **图标体系**：当前使用 emoji（🔧📦⚙️），需统一替换为 SVG 图标库（建议：Lucide 或自定义），保证跨平台一致性
- [ ] **滚动条样式**：全局自定义滚动条（webkit-scrollbar），统一为 6px 宽度、圆角、hover 变深
- [ ] **空态插图**：收藏夹/列表为空时，使用 SVG 插图替代纯文字，提升视觉质感
- [ ] **Loading 状态**：各模块 loading 动画统一，避免多种不同风格的跳动圆点
- [ ] **错误边界**：增加 React Error Boundary，防止单组件错误导致整个应用白屏

### 9.3 待优化（中优先级）

- [ ] **键盘快捷键**：支持 `Ctrl+K` 聚焦搜索、`Esc` 关闭弹层、`↑/↓` 在列表中导航
- [ ] **拖拽排序**：收藏夹中的微应用支持拖拽调整顺序
- [ ] **深色模式**：基于 Design Tokens 扩展 dark mode（电力监控场景常需夜间值班）
- [ ] **虚拟滚动**：当微应用/接口数量超过 100 时，列表启用虚拟滚动优化性能
- [ ] **骨架屏**：首次加载数据时，用骨架屏替代空白或 loading 圆点

### 9.4 待优化（低优先级 / 未来）

- [ ] **响应式完善**：平板端（<1280px）的 Sidebar 抽屉式交互
- [ ] **PWA 支持**：离线查看已加载的微应用列表和消息历史
- [ ] **国际化**：提取所有中文文案，支持 i18n（为后续海外园区场景做准备）
- [ ] **操作引导**：新用户首次进入时，增加 Spotlight 式的产品导览

---

## 10. 附录

### 10.1 组件清单

| 组件 | 路径 | 职责 |
|------|------|------|
| Layout | components/Layout.tsx | 三栏布局骨架 |
| AppList | components/Sidebar/AppList.tsx | 微应用列表 + tooltip |
| ChatWindow | components/Chat/ChatWindow.tsx | 对话主区域 |
| ApiList | components/ApiCatalog/ApiList.tsx | 服务目录 |
| FlowChart | components/Common/FlowChart.tsx | 流程图渲染 |
| Toast | components/Common/Toast.tsx | 全局消息提示 |
| TimerPanel | components/Common/TimerPanel.tsx | 时钟（未挂载，预留） |

### 10.2 API 依赖

```
GET  /api/data/current          → 当前场景域
GET  /api/micro-apps?source=xxx → 微应用列表
POST /api/micro-apps            → 创建微应用
PUT  /api/micro-apps/:id        → 更新微应用
DELETE /api/micro-apps/:id      → 删除微应用
POST /api/micro-apps/:id/execute → 执行微应用
GET  /api/apis                  → API 目录
GET  /api/skills                → Skill 列表
GET  /api/skills/:name          → Skill 详情
GET  /api/chat/stream           → SSE 对话流
DELETE /api/chat/history        → 清空对话历史
```

---

## 11. 评审记录

| 日期 | 评审人 | 意见 | 状态 |
|------|--------|------|------|
| 2026-04-23 | — | 初稿 | 待评审 |
